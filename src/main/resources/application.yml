spring:
  application:
    name: olist-eda-dashboard-spring
  sql:
    init:
      # The analytics database is externally supplied and always opened read-only.
      mode: never

olist:
  database:
    path: ${OLIST_DB_PATH:}
  cors:
    origins: ${CORS_ORIGINS:}
